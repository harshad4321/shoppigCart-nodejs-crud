<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div id='navbarId' class="container-fluid">
    <a class="navbar-brand" href="/user/my-profile">
      {{#if user}}
      Welcome
      {{user.name}}
      {{else}}
      Account
      {{/if}}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/">products
            <span class="sr-only">(current)</span></a>
        </li>

        {{#if user}}
        <li class="nav-item">
          <a class="nav-link" href="/cart">cart <span class="badge badge-success"
              id="cart-count">{{cartCount}}</span></a>
        </li>
        {{else}}
        <li class="nav-item">
          <a class="nav-link" href="/user/signup">cart <span class="badge badge-success" id="cart-count">0</span></a>
        </li>
        {{/if}}
        {{#if user}}
        <li class="nav-item">
          <a class="nav-link" href="/orders">Orders</a>
        </li>
        {{/if}}
        </li>
        <li class="nav-item mr-4">
          <a class="nav-link disabled" href="/">Disabled</a>
        </li>
      </ul>
      <form class="form-inline my-2 my-lg-0">
        <div class="dropdown">
          <button class="btn btn-secondary dropdown-toggle btn btn-outline-success mr-4"
            class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
            aria-haspopup="true" aria-expanded="false">
            {{#if user}}
            Welcome
            {{user.name}}
            {{else}}
            Account
            {{/if}}
          </button>
          </a>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            {{#if user}}
            <a class="dropdown-item" href="/user/logout">Logout</a>
            {{else}}
            <a class="dropdown-item" href="/user/login">Login</a>
            <a class="dropdown-item" href="/user/signup">signup</a>
            {{/if}}
          </div>
        </div>
      </form>
    </div>
  </div>
</nav>